const a=10;  //allocate memory for a number;

const employee={
    name:'dan'
}

function substract(num){
    return num-2;
}
function sum(){
    const total=4+4;
    return substract(total);
}
debugger;
sum();
// console.log(employee);

//this exemple is for pasting in browser inspect elements as a snippet to see the order of the functions called in stack

